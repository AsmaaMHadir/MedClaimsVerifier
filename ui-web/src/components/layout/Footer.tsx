export function Footer() {
  return (
    <footer className="mt-24 border-t border-rule">
      <div className="mx-auto flex max-w-5xl flex-col items-start justify-between gap-2 px-6 py-8 text-xs text-ink-dim sm:flex-row sm:items-center">
        <span className="font-serif text-sm text-ink">MedVerify</span>
        <span>Evidence-checked medical claim verification.</span>
      </div>
    </footer>
  );
}
