# MedVerify API - Requirements
# pip install -r requirements.txt

# === Core API ===
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
python-multipart>=0.0.6

# === Database ===
neo4j>=5.16.0,<6.0.0

# === HTTP/Async ===
httpx>=0.26.0

# === Utilities ===
python-dotenv>=1.0.0
loguru>=0.7.0

# === Security & Performance ===
slowapi>=0.1.9
cachetools>=5.3.0

# === NLP / Entity Extraction ===
gliner>=0.2.0
numpy>=1.26.4  # Required for gliner compatibility

# === Testing ===
pytest>=7.4.0
pytest-asyncio>=0.23.0

# === Dev Tools ===
black>=23.12.0
ruff>=0.1.9

# === UI ===
streamlit>=1.32.0
pyvis>=0.3.2

# NOTE: GLiNER is a lightweight zero-shot NER model that runs locally.
# It automatically downloads the model (~500MB) on first use.
