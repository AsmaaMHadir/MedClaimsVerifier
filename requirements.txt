# MedVerify API - Requirements
# pip install -r requirements.txt

# === Core API ===
fastapi>=0.109.0,<1.0.0
uvicorn[standard]>=0.27.0
pydantic>=2.5.0,<3.0.0
pydantic-settings>=2.3.0,<3.0.0
python-multipart>=0.0.6

# === Database ===
neo4j>=5.16.0,<6.0.0

# === HTTP/Async ===
httpx>=0.26.0

# === Utilities ===
python-dotenv>=1.0.0
loguru>=0.7.0

# === Security & Performance ===
slowapi>=0.1.9
cachetools>=5.3.0

# === NLP / Entity Extraction ===
gliner>=0.2.0
numpy>=1.26.4  # Required for gliner compatibility

# === ML / Embeddings (SapBERT lay-term canonicalisation layer) ===
# Imported lazily by src/services/sapbert_normalizer.py on first KG miss.
# Without these, SapBERT fallback raises ModuleNotFoundError mid-request.
transformers>=4.40.0,<5.0.0
torch>=2.2.0,<3.0.0

# === LLM Predicate Fallback ===
# Imported lazily by src/services/llm_predicate_resolver.py. Pulls boto3
# transitively via the [bedrock] extra so AWS Bedrock support works
# out-of-the-box; the verifier degrades gracefully when no key is set.
anthropic[bedrock]>=0.34.0,<1.0.0

# === Evaluation harness ===
pyyaml>=6.0,<7.0

# === Testing ===
pytest>=7.4.0
pytest-asyncio>=0.23.0

# === Dev Tools ===
black>=23.12.0
ruff>=0.1.9

# === UI ===
streamlit>=1.32.0
pyvis>=0.3.2

# NOTE: GLiNER weights (~1-2 GB for the biomed-large default) and SapBERT
# weights (~440 MB) download from Hugging Face on first use.
